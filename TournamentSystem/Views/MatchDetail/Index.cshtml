@model IEnumerable<TournamentSystem.Models.MatchDetail>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScoreTeam1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScoreTeam2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScoreTeam1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScoreTeam2)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MatchDetailId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MatchDetailId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MatchDetailId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
